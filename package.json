{
  "name": "day13",
  "version": "1.0.0",
  "description": "We know how to place event handlers on HTML elements in our javascript, and then listen for them, and fire arbitrary code when that happens.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify index.js -p esmify > bundle.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.1",
    "browserify": "^17.0.0",
    "ethers": "^5.7.2",
    "web3": "^1.8.1"
  },
  "devDependencies": {
    "esmify": "^2.1.1"
  }
}
